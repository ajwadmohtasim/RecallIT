services:
  mongo:
    image: mongo:7
    container_name: recallit-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - recallit_mongo_data:/data/db

volumes:
  recallit_mongo_data:
